const nameArray = ["Bhoomika", "Guni", "Shreya"]

for(const name of nameArray)
    console.log(name);

nameArray.push("Palak");     // add a name at the end of the array

for(const name of nameArray)
    console.log(name);
